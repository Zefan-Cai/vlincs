# CPU-only. No torch, no ultralytics, no model weights — the gallery matches on the embeddings YOU push.
numpy
pandas
pyarrow
scikit-learn
psycopg[binary]
pgvector
faiss-cpu                       # in-process exemplar k-NN — needed for match_mode="retrieval" + large-gallery scale
                                # (default match_mode="centroid" uses exact numpy mean-cosine; faiss not used there)
opencv-python-headless          # viz crops from the source video
fastapi
uvicorn[standard]
# reid_hota = the canonical TA1 scorer (IDF1/HOTA). Internal package; at build time install from the
# Novateur devpi index, e.g.:
#   pip install --index-url http://euclid.novateur.com:48731/novateur/dev/+simple/ \
#               --trusted-host euclid.novateur.com reid_hota
# (or drop a reid_hota wheel into kit/vendor/ and `pip install kit/vendor/reid_hota-*.whl`).
reid_hota
